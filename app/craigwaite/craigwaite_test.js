'use strict';

describe('myApp.craigwaite module', function () {

    beforeEach(module('myApp.craigwaite'));

    describe('Craigwaite controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var view1Ctrl = $controller('CraigwaiteCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});